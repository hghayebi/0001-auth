import { login } from "./login";
import { logout } from "./logout";
import { newVerification } from "./new-verification";
import { register } from "./register";

const actions = { register, login, logout, newVerification };

export default actions;
